import clsx from "clsx";
import { BirdIcon } from "lucide-react";

const NestIcon = () => {
  return <BirdIcon className={clsx("w-4 h-auto text-gray-500 dark:text-gray-400")} />;
};

export default NestIcon;
